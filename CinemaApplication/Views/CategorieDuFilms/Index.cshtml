@model IEnumerable<CinemaApplication.Models.CategorieDuFilm>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GenreDuFilm)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GenreDuFilm)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CategorieDuFilmId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CategorieDuFilmId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CategorieDuFilmId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
